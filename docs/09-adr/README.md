# 09-adr — Architecture Decision Records

การตัดสินใจเชิงสถาปัตยกรรมที่ย้อนดูได้ 1 decision = 1 ไฟล์ `adr-<nnnn>-<slug>.md` (ใช้ `_template.md`)

- เลข run ต่อเนื่อง 0001, 0002 …
- code commit อ้างกลับได้ (`ADR-0003`) → traceability docs↔code
- ไม่ลบของเก่า — decision ที่เลิกใช้ mark `Superseded by ADR-xxxx`
