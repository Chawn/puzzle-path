# 03-domains

Design doc ต่อ feature/module — หัวใจของ docs. ทุกไฟล์ใช้ `_template.md` (มี Out of scope + Acceptance Criteria พร้อม handoff Codex)

- 1 feature = 1 ไฟล์ `<name>.md`
- Title ลงท้าย `— DESIGN` (ยังไม่ทำ) → `— IMPLEMENTED` (เสร็จ + as-built)
